# PLAYTEST

Ориентир: ~60 FPS в консоли Периметра. Если кадры падают «в пол» — шаг не засчитан.

## Фаза 1 — каркас вылазки

1. В корне: `npm install`, затем `npm run dev`.
2. Открыть URL Vite. Видна плоскость, туман, оливковая капсула «Новичок».
3. Справа в консоли Периметра включён FPS (число кадров).
4. WASD — капсула бежит по земле. Правая кнопка мыши — вращение камеры. Колесо — зум.
5. ЛКМ по земле — капсула идёт в точку, виден жёлтый маркер.
6. Подойти к ржаво-красной капсуле «Хряк» (имя и полоска HP над головой).
7. Нажать Tab, затем `1`. Всплывает цифра урона, в логе строка, HP хряка падает.
8. GM **Heal** — HP/выносливость полные.
9. GM **Level Up** — ранг растёт, тост «Ранг повышен».
10. GM **Teleport Forward** — капсула смещается вперёд.
11. `npm run build` без ошибок TypeScript.

## Фаза 2 — ходить и целиться

1. На площадке стоят **5 хряков**, не один.
2. WASD, ПКМ-камера, клик-мув с жёлтым маркером работают как в фазе 1. WASD сбрасывает маркер.
3. Нажать **Tab** — выбирается ближайший живой хряк: он чуть светлее, сверху по центру КПК рамка **Цель** (имя, уровень, HP).
4. Без Tab клавиша `1` пишет, что нет цели.
5. Подойти к выбранному, нажать `1` — бьётся он, не сосед.
6. Убить выбранного: он исчезает, остальные остаются, рамка цели пропадает.
7. Tab снова цепляет ближайшего из живых.
8. GM **Spawn Monster** добавляет хряка, пока их меньше 5; при пяти — «Хватит хряков».
9. FPS не должен просесть «в пол» относительно фазы 1.

## Фаза 3 — бой и ранг

1. Tab, подойти к хряку, нажать `1` — урон по хряку как раньше.
2. Стоять рядом: хряк бьёт в ответ (не чаще чем раз в ~1.5 с). HP на КПК падает, в логе «Хряк нанёс N урона», цифра над игроком.
3. Если HP доходит до 0 — одно сообщение «Новичок без сознания». GM **Heal** поднимает.
4. Убить несколько хряков: опыт копится, при пороге — тост «Ранг повышен», max HP/выносливость/атака растут.
5. GM **+1000 Exp** — сразу много опыта и, скорее всего, несколько рангов.
6. После пачки смертей FPS не ползёт вниз (трупы исчезают).

## Фаза 4 — заказ у Кефира

1. Слева/сзади от старта (не в пачке хряков) стоит грязно-жёлтый цилиндр **Барыга Кефир**.
2. Издалека `F` — лог «Подойдите ближе». Подойти, `F` — диалог, **Закрыть** и Esc гасят окно.
3. **Принять заказ** — справа трекер «Хряки у депо» 0/3.
4. Убить трёх хряков: трекер 1/3 → 3/3, строка «Заказ можно сдать».
5. Снова `F` у Кефира — **Получить награду**. Опыт +120, в логе малая аптечка, заказ сдан, трекер пропадает.
6. Повторно награду за тот же заход нельзя. После сдачи заказ можно взять снова.
7. GM **Complete current quest**: если заказ не готов — сразу 3/3; если уже 3/3 — выдаёт награду.
8. FPS не должен просесть «в пол».

## Фаза 5 — КПК

1. `I` открывает инвентарь (малая аптечка, мясо хряка, количества). `I` или Esc / **Закрыть** гасят окно. Пока открыт диалог Кефира, `I` не открывает инвентарь.
2. Получить удар хряка (HP не полное). GM **Give medkit**, `I`, **Использовать**: HP +30, не выше максимума. При полном HP аптечка не тратится. С 0 штук кнопка не расходует предмет.
3. Убить хряка — в инвентаре мясо +1. Кнопки «Использовать» у мяса нет.
4. Справа сверху 2D-миникарта: белая точка — ты, красные — хряки, жёлтая — Кефир, зелёное кольцо — цель заказа (Кефир до взятия/на сдаче, ближайший хряк пока заказ active и не 3/3). После сдачи зелёное снова на Кефире (заказ можно взять ещё раз).
5. FPS не должен просесть «в пол».

## Фаза 6 — иллюзия Периметра

1. На площадке **6** капсул с позывными: Ржавый, Окурок, Плут, Газик, Кочегар, Лом. Ходят и иногда стоят. На миникарте — тусклые оливковые точки.
2. Внизу рация: вкладки Система / Периметр / Бой. Периметр сам пишет фразы ботов. Написать в поле, **В эфир** — в канале своя строка, через секунду-две отвечает бот. Бой и система — без ввода. Список хронологический: старые сверху, новые внизу. Новая строка (бот, свой эфир) и смена вкладки с сообщениями всегда прокручивают к последней, даже если крутили вверх. Если строк больше окна — вертикальный скролл списка, колесо крутит как обычный чат. FPS в GM не должен просесть «в пол».
3. Кнопка **Авто** на рамке Новичка: идёт к Кефиру, берёт заказ, бьёт трёх хряков, сдаёт, берёт снова. В Системе строки `[Авто]`. WASD или клик по земле выключают Авто.
4. GM **Teleport to quest target** ставит капсулу к зелёной цели карты.
5. FPS при ботах + хряках не должен просесть «в пол».

## Фаза 7 — чтобы играть, не читая код

1. Взять заказ и/или аптечку, отойти. F5: ранг, HP, заказ, инвентарь и место на площадке на месте.
2. GM **Reset Save** — снова Новичок, заказ available, пустой инвентарь.
3. GM **AUTO TEST** (не путать с **Авто**): капсула сама берёт заказ, бьёт трёх, сдаёт. В Системе `[AUTO TEST] OK`. Повторный клик, пока идёт прогон, ничего не делает. Если прервать WASD — `[AUTO TEST] FAIL`.
4. У Кефира столбы и трубы, у хряков в `+X/+Z` — кубы ржавого депо. Не пустая плоскость.
5. FPS не должен просесть «в пол».

## Фаза 8 — графика из паков

1. В Системе строка **Насыпь: паки Kenney.** (если файлов нет — **кубы**, шаг не зелёный).
2. GM **Teleport to depot**: в `+X/+Z` склады и контейнеры, не набор кубов. У Кефира — дымоходы пака, не пустая плоскость.
3. Новичок, хряки, Кефир и боты по-прежнему капсулы/цилиндр. Mixamo нет.
4. FPS в GM не должен просесть «в пол» относительно фазы 7. Если падает — паки выкинуть, вернуть кубы.
5. `npm run build` без ошибок.

## Камера — ПКМ (Babylon 9)

В Babylon 9 RMB по умолчанию панорамирует, а не крутит. Панорама выключена (`panningSensibility = 0`), поэтому без ремапа `movement.input.setInteraction` ПКМ ничего не делала.

1. ПКМ-drag орбитирует камеру вокруг капсулы. Капсула остаётся целью follow-камеры.
2. ЛКМ камеру не крутит. Клик по земле ставит жёлтый маркер, как раньше.
3. Колесо зумит (радиус примерно 4–22).
4. FPS в GM не должен просесть «в пол».

## ЛКМ — клик-мув

Короткий клик по **открытой земле** (не по рации слева снизу, не по консоли справа, не по рамке слева сверху). Если мышь сдвинулась больше чем на ~6 px, ход не ставится — это фильтр, не баг. ЛКМ не атакует и не крутит камеру.

1. Кликнуть в **центр** пустой земли на канвасе. Появляется жёлтый диск, капсула идёт к нему.
2. WASD по-прежнему ходит; WASD сбрасывает маркер.
3. FPS в GM не должен просесть «в пол».

## Фаза 9 — HTTP API

Без сервера клиент как в фазах 6–8: `npm run dev`, логина на КПК нет.

Нужны локальный Postgres (база `shatalker`) и Go. Docker не использовать.

1. `cp server/.env.example server/.env`. `make migrate-up`, затем `make serve`. `curl http://localhost:8080/api/v1/health` → `{"ok":true}`.
2. `cp .env.example .env.local` и раскомментировать `VITE_API_URL=http://localhost:8080`. Перезапустить `npm run dev`. На КПК поля позывной/пароль.
3. **Регистрация**, вылазка (заказ или аптечка), GM **Push save**. GM **API Health** пишет ok.
4. GM **Logout**, снова **Войти** (или другой браузер/профиль): ранг, заказ, инвентарь, место с сервера. **Pull save** подтягивает снимок.
5. `make build` и `npm run build` без ошибок. FPS не должен просесть «в пол» (сейв не каждый кадр).

## Фаза 10 — живая рация

Без `VITE_API_URL` и без логина — как в фазах 6–8: `npm run dev`, рация только локальная.

С API (Postgres + `make serve`, клиент с `VITE_API_URL`):

1. Два окна (или два профиля): два разных аккаунта, оба «В сети». В консоли Периметра **WS: вкл**, в Системе строка `[API] рация WS`.
2. В одном окне написать в «Периметр» и отправить. Во втором та же реплика с чужим позывным, без Pull. У отправителя строка появляется один раз (с эфира, не дубль).
3. Подождать бота в рации: его фраза есть только в своём окне, во втором не появляется.
4. Выйти (GM **Logout**) — **WS: выкл**. Без сервера игра по-прежнему стартует.
5. FPS в GM не крутится от сокета (нет просадки «в пол» на фоне рации).

## Фаза 11 — комната «Насыпь»

Без API — как в фазах 6–8: списка «На насыпи» нет, чужих капсул нет, боты только локальные.

С API (`make serve`, `VITE_API_URL`):

1. Два окна, два аккаунта, оба «В сети». На КПК блок **На насыпи** с чужим позывным (и своим).
2. На площадке вторая оливковая капсула с подписью. Подойти — она не хряк (Tab её не берёт). Походить: чужая капсула смещается не каждый кадр, без рывков «20 раз в секунду».
3. Боты в рации по-прежнему только в своём окне. Бой и хряки локальные.
4. GM **Logout** — список пустеет, чужая капсула пропадает. Без сервера игра стартует.
5. FPS в GM не должен просесть «в пол» (позиции не 20 Гц и не в React).

## Фаза 12 — прод (Docker)

Локально по-прежнему `make serve` **без** Docker и локальный Postgres.

1. `make build` и `npm run build` без `VITE_API_URL` — клиент офлайн, как в 6–8.
2. `make image` (или `docker compose -f docker-compose.prod.yml build`). Образ API не гоняет goose при старте.
3. `cp deploy/.env.prod.example deploy/.env.prod`, задать `JWT_SECRET`. `docker compose -f docker-compose.prod.yml --env-file deploy/.env.prod up -d`. Сервис `migrate` отрабатывает и выходит, потом `api` и `web`.
4. `curl http://localhost/api/v1/health` → `{"ok":true}`. В браузере `http://localhost` — логин на КПК, рация WS (прокси `/api/`). HTTPS — снаружи, не в compose.
5. `make serve` на машине разработчика Docker не требует.

## Фаза 13 — небо и земля

Туман, Kenney и цикл заказа как в фазе 8. Купол — не HDRI 2K+ и не второе солнце. Текстура неба: `public/assets/tex/sky_overcast_512.jpg`.

1. `npm run dev`. Над Насыпью пасмурные облака на куполе (не сплошная заливка `clearColor`), у горизонта стык с туманом.
2. Земля 80×80 не однотонный «пластилин»: грязные пятна, к краю чуть темнее.
3. Клик в центр земли — жёлтый диск клик-мува. Купол клик не перехватывает.
4. GM **Toggle sky** — купол гаснет (снова заливка тумана) и включается обратно. В логе «Купол вкл/выкл».
5. FPS в GM не должен просесть «в пол» относительно фазы 8. Space прыгает (не в поле рации).

Модели — только файлы из `/public/assets` (Kenney CC0).

## Фаза 14 — декор Периметра

Тот же 80×80. 14.3 нет: лишних Kenney glb в лицензии нет.

1. С края площадки видны редкие столбы и обломки (не сплошной забор). Купол и клик-мув как в 13.
2. Три тусклые сферы «слабая аномалия» — светятся без фонариков и частиц. Сквозь сферу проходишь.
3. На земле упираешься в столб края. **Space** — подскок, столб перелетается; склад депо — нет.
4. GM **Toggle anomalies** гасит сферы. **Show colliders** рисует и край.
5. FPS в GM не должен просесть «в пол» относительно фазы 13. AUTO TEST по-прежнему сдаёт заказ.

## Фаза 15 — столкновения (не Havok)

Havok и плагинная физика не подключены. Прыжок — парабола по Y, не движок физики.

1. `npm run dev`. WASD до края площадки — капсула не уходит в туман за 80×80. Клик за край ставит маркер на земле, не в пустоте.
2. GM **Teleport to depot**, обойти склад: капсула скользит вдоль, не оказывается внутри меша. WASD вдоль стены не застревает на шаге ходьбы.
3. Клик по земле у стены (или «внутрь» склада) — жёлтый маркер снаружи. Авто и **AUTO TEST** по-прежнему сдают заказ у Кефира.
4. GM **Show colliders** — полупрозрачные оливковые боксы по складам и краю; повторный клик гасит. Хряки и боты не ходят сквозь те же ящики.
5. FPS в GM не должен просесть «в пол» относительно фазы 13.
