# Графика

Живые актёры — **примитивы Babylon** (капсулы, цилиндры). Статика Насыпи — подмножество Kenney City Kit (Industrial) в `/public/assets/kenney/industrial`. Нет файла — снова куб/столб из фазы 7.

Картинка «из головы модели» **не** встаёт в движок с нужным метром, UV и бюджетом. Сначала спецификация, потом файл, потом код. Не наоборот.

---

## 1. Откуда берём

| Этап | Что | Зачем |
| --- | --- | --- |
| Сейчас | Актёры-примитивы, статика Kenney CC0 | FPS, нет битых путей |
| КПК / иконки | Обычный CSS, при необходимости PNG из спеки | Лёгкий UI, без Tailwind |
| Дальше | Mixamo / тайлы, только если FPS в запасе | Не раньше живых паков |

Паки (только generic industrial / ruined, без чужих героев и локаций):

- **Kenney** — простые пакеты
- **Poly Pizza** / **Quaternius** — low-poly
- **Mixamo** — анимации, не раньше, чем есть персонаж
- **ambientCG** — тайловые материалы
- **OpenGameArt** — мелочи, смотреть лицензию

Всё класть в `/public/assets/...` с понятными именами. В коде — только существующие пути. Нет файла — снова примитив.

---

## 2. Что умеет генерация картинок

Можно сгенерировать **2D**: концепт, иконку КПК, пятно ржавчины, эскиз.

Нельзя сгенерировать как готовый игровой ассет:

- модель `.glb` / скелет / анимацию;
- нормали, roughness, AO как отдельные корректные каналы;
- бесшовный тайл «с первого раза»;
- пачку ассетов в одном стиле и одном масштабе без ручной приёмки.

**Точно соблюдать размеры и требования графики генерация не будет.** Она даёт картинку с соотношением сторон (`1:1`, `16:9`…), не лист спеки движка. Дальше всё равно: кадрирование, ресайз до **степени двойки** (256 / 512 / 1024), проверка веса, иногда дорисовка шва.

Поэтому генерация — черновик. В игру попадает только файл, который прошёл спеку ниже.

---

## 3. Спека (когда файлы появятся)

- **1 единица Babylon = 1 метр.** Капсула игрока ~1.8 м. Хряк чуть ниже и длиннее. Не масштабировать «на глаз» каждую модель.
- Текстуры: квадрат, power-of-two, сначала **512**, не 4K. Сжатие потом (не раздувать PNG).
- Одна текстура на материал на старте (albedo). Без набора из 5 карт на ящик.
- Бюджет сцены: мало уникальных материалов, примитивы и паки low-poly, не «фото меша».
- Иконки UI: один размер на набор, например **64×64** или **128×128**, одинаковый паддинг.
- Имена: `public/assets/tex/dirt_512.png`, `public/assets/tex/sky_overcast_1k.jpg`, не `image (3).png`.
- Лицензия пака — в `public/assets/LICENSES.md`, когда появятся чужие файлы.

Если ассет тяжелее примитива и FPS падает — выкинуть ассет, вернуть капсулу. См. [performance.md](./performance.md).

---

## 4. Небо, земля, аномалии (фазы 13–14)

Не выдумывать пути. Нет файла — примитив / вертексный цвет.

| Что | Как дёшево | Чего нет |
| --- | --- | --- |
| Небо | Перевёрнутая полусфера, `tex/sky_overcast_1k.jpg` (эквирект 1024×512) + градиент к туману | HDRI/EXR в рантайме, Skybox 2K+, закат, второе солнце |
| Земля | Один albedo 512, тайлинг, путь вида `public/assets/tex/dirt_512.png` | 4K, normal+roughness+AO пачкой |
| Аномалия | 2–4 сферы, emissive на `StandardMaterial` | Частицы, glow layer, point light на каждую |

Купол и декор: `isPickable = false`, иначе клик-мув сломается. Коллизия — фаза 15, не «физика меша».
